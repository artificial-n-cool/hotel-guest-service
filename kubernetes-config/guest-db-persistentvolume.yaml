apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    app: guest-pv
    type: local
  name: guest-pv-volume
spec:
  storageClassName: manual
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"